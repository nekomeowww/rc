---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: workspaceenvironments.workspaces.rc.ayaka.io
spec:
  group: workspaces.rc.ayaka.io
  names:
    kind: WorkspaceEnvironment
    listKind: WorkspaceEnvironmentList
    plural: workspaceenvironments
    shortNames:
    - env
    singular: workspaceenvironment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.currentRevision
      name: Revision
      type: integer
    - jsonPath: .status.currentImage
      name: Image
      type: string
    - jsonPath: .status.currentVolumeClaimName
      name: Current PVC
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkspaceEnvironment is the Schema for the workspaceenvironments
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WorkspaceEnvironment
            properties:
              commit:
                description: commit requests promotion of draft when incremented by
                  rcctl env commit.
                format: int64
                minimum: 0
                type: integer
              editorIdleTimeout:
                description: |-
                  editorIdleTimeout controls how long an idle editor Pod remains running.
                  Zero disables automatic suspension.
                type: string
              image:
                description: image is the runner image string copied into each new
                  Workspace.
                minLength: 1
                type: string
              storage:
                description: storage configures current and draft home volumes.
                properties:
                  accessModes:
                    description: |-
                      accessModes controls how the volume may be mounted. rc defaults to
                      ReadWriteOnce when this field is omitted.
                    items:
                      type: string
                    type: array
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    description: size is the requested storage capacity.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                    x-kubernetes-validations:
                    - message: size must be greater than zero
                      rule: quantity(self).isGreaterThan(quantity('0'))
                  storageClassName:
                    description: |-
                      storageClassName is the StorageClass used to provision the volume. When
                      omitted, Kubernetes selects the cluster default StorageClass.
                    type: string
                  volumeMode:
                    description: |-
                      volumeMode controls whether the volume is exposed as a filesystem or a
                      block device. Workspace runtime volumes require Filesystem.
                    enum:
                    - Filesystem
                    type: string
                required:
                - size
                type: object
            required:
            - image
            - storage
            type: object
          status:
            description: status defines the observed state of WorkspaceEnvironment
            properties:
              committedRequest:
                description: committedRequest is the latest spec.commit value successfully
                  promoted.
                format: int64
                type: integer
              conditions:
                description: |-
                  conditions represent the current state of the WorkspaceEnvironment resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentImage:
                description: currentImage is the exact image string associated with
                  currentRevision.
                type: string
              currentRevision:
                description: currentRevision is the committed content revision cloned
                  by new Workspaces.
                format: int64
                type: integer
              currentVolumeClaimName:
                description: currentVolumeClaimName is the PVC cloned by new Workspaces.
                type: string
              draftVolumeClaimName:
                description: draftVolumeClaimName is the mutable draft PVC, when one
                  exists.
                type: string
              editorPodName:
                description: editorPodName is the active Environment editor Pod, when
                  one exists.
                type: string
              observedGeneration:
                description: observedGeneration is the latest generation reflected
                  by status.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
