# This rule is not used by the project rc itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants full permissions ('*') over workspaces.rc.ayaka.io.
# This role is intended for users authorized to modify roles and bindings within the cluster,
# enabling them to delegate specific permissions to other users or groups as needed.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: rc
    app.kubernetes.io/managed-by: kustomize
  name: workspaces-agentprocess-admin-role
rules:
- apiGroups:
  - workspaces.rc.ayaka.io
  resources:
  - agentprocesses
  verbs:
  - '*'
- apiGroups:
  - workspaces.rc.ayaka.io
  resources:
  - agentprocesses/status
  verbs:
  - get
